BlobVision V1 — local model weights (not in git).

Subfolders:
  sdxl-turbo/       SDXL Turbo fp16 (~6.5 GB) — Redux / DeepDream sketch
  vqgan/            VQGAN checkpoint + yaml
  open_clip/        OpenCLIP weights cache
  hf_cache/         Hugging Face hub cache
  style-transfer/   VGG19 vgg19_imagenet.pth (~548 MB)

Install via BlobVision.bat (in-app Install buttons) or app/scripts/download_*.py
